#create a new db if starting new. name it camops_db
# Test comment for git author verification
# --- Member 4: OAuth2 (Google) and JWT (set env vars in production) ---
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:120706363738-rj6o97eivmfjjlo7nfdv14hfb6tqktar.apps.googleusercontent.com120706363738-rj6o97eivmfjjlo7nfdv14hfb6tqktar.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET: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}
spring.security.oauth2.client.registration.google.scope=openid,profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}
spring.security.oauth2.client.provider.google.user-name-attribute=sub
app.jwt.secret=${APP_JWT_SECRET:camops-dev-jwt-secret-key-please-change-in-prod-32chars!}
app.jwt.expiration-ms=86400000
app.frontend.url=${FRONTEND_URL:http://localhost:5173}
# Used to build the Google OAuth start URL in API responses (browser must open this on the backend port)
app.backend.base-url=${BACKEND_BASE_URL:http://localhost:8080}
# Set true for tests or local experiments only (keeps /api unauthenticated)
app.security.permit-all=${APP_SECURITY_PERMIT_ALL:false}

spring.datasource.url=jdbc:mysql://localhost:3306/campus_db?serverTimezone=UTC

# add YOUR db info here
spring.datasource.username=root
spring.datasource.password=10131

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true